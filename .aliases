# ##############################################
# The aliases file is used to setup all aliases 
# to quickly access to any command or resource
# ##############################################

# 
alias browsethis="xdg-open ."

# laravel
alias lumen_new="composer create-project --prefer-dist laravel/lumen" 
alias laravel_new="composer create-project --prefer-dist laravel/laravel"

# github
alias gif="git diff "
alias gip="git push origin "
alias gcm="git commit -m "
alias gad="git add "
